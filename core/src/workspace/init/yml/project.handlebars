# a demo project created by the `init` command.
targets:
  # you can try to launch this target with the following command :
  # blaze run example-project:say-hello
  say-hello:
    executor: std:commands
    options:
      shell: true
      commands:
        - program: echo
          arguments:
            - '\{{ vars.demo-message }}'
